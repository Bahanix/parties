%label.checkbox{ for: "items_#{i.id}" }
  = check_box_tag "items[#{i.id}]", 1, false, class: 'item', 'data-id' => i.id
  = i.name
  - i.participants.each do |p|
    = render 'participants/avatar', p: p
